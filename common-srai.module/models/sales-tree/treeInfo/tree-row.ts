export class TreeRow {
  public id; public parentId; public content;
  constructor(id: any, parentId: any, content: any) {
    this.id = id;
    this.parentId = parentId;
    this.content = content;
  }
}
